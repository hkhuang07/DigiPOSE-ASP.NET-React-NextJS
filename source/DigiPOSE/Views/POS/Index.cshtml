@{
    ViewData["Title"] = "POS Terminal";
    // For POS Terminal we might want a different Layout or no layout at all, but for now we use the main layout if we don't specify, or a specific POS layout.
    // Layout = null; // Un-comment if you want a standalone full-screen UI without sidebar.
}

<div class="container-fluid py-4">
    <div class="row">
        <div class="col-12 text-center">
            <h1 class="text-cyan mb-4" style="font-family: 'Orbitron', sans-serif;"><i class="fa-solid fa-cash-register"></i> POS TERMINAL</h1>
            <p class="text-muted">Point of Sale interface is under construction.</p>
        </div>
    </div>
</div>

